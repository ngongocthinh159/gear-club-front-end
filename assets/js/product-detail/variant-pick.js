// console.log("entered")

// const optionSelectors = document.querySelectorAll("[data-option-selector]");

// console.log(optionSelectors)

// // Add event listner on change
// optionSelectors.forEach ( (optionSelector) => {
//   optionSelector.addEventListener("change", onOptionChanged)
// })

// function onOptionChanged(event) {
//   console.log(event)

//   if (!event.target.name.startsWith("option")) {
//     return;
//   }

//   console.log(event.target.id)

//   arr = Array.from(optionSelectors);

//   console.log(arr.indexOf(event.currentTarget))

//   let options = arr[arr.indexOf(event.currentTarget)].querySelectorAll('label')

//   for (let option of options) {
//     if (option.id == event.target.id) {
//       option.innerHTML += `<style> border-width: medium;`;
//       option.classList
//     }
//     else {}
//   }

//   selectVariant(event.target.id);
// }

// function selectVariant(id) {
  
//   // let curOption = optionSelectors[]
// }
